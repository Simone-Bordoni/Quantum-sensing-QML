Run experiment:
+ per far usare measurement protocol usare aggregazione per capire cosa far vedere:
    media per esperimento persistente
    massimo per evento transiente
    matrice confusione misurata con (prod(c_ii))^{1/n} dove c_ii sono elementi diagonali (ma matrice di confusione non +è ancora formata???)
+ alla fine dare matrici di confusione per ogni misura
+ calcolare la probabilità di falsi allarmi per ogni configurazione. 
    (per la configurazione i la probabilità che almeno una volta non esca config_i o ground, quindi serve dare informazione su chi è ground state)

MeasurementProtocol:
+ cambiare nome per includere tempo di inizio simulazione, che se non è incluso qua è facile da dimenticare
+ mettere toggle di rumore su inizio simulazione true di default

Callback:
+ salvare metadata dell'esperimento

Generale:
+ mettere modulazione temporale delle hamiltoniane insieme a quelle dei lindblad durante la creazione dei termini
+ spostare creazione hamiltoniana in quantum utils
+ capire come gestire esperimenti transienti che potrebbero "attivare" la misura a tempi differenti
+ permettere di avere rumore anche su inizio sim
+ selezionare ground state
- Change readme fix documentation and tests

Sweep:
+ test
+ rendere possibile sul rumore
- auto-resolution

Optimization:
+ TEST
+ riparare metriche finali matrici di confusione
+ run experiment alla fine non usa rumore, correggere le simulazioni per usare qualsiasi measurement protocol
+ custom precision annealing
- problema: quando validation raggiunge il massimo presto, la precisione è poca ed il risultato è inesatto.
- optimize measurement time
- optimize promotable parameters
- speedup??

TimeEvolution:
- tiene traccia di operatore personalizzato

Visualization:
+ multidimensional sweep visualization
+ aggiungere ground e perturbation_type al print di exp_params
+ make protocol printa ottimizzazione al posto di make protocol o simulazione singola
- Time evolution where various subsystems and observables can be choosen and toggled, including various time modulations
- clean and automize optimization dashboard (wheter or not detection metrics should be plotted and wheter or not visualization and metric should both be plotted)

DetectionMetric:
- rielaborare il MeasurementAggregator per poter avere metriche che massimizzano ottenere risultati positivi su k misure su n oppure k misure consecutive. 
    Serve tener conto di un vettore di k successi e non basta un float.
    Per k successi creare vettore di tutti i possibili gruppi di k successi differenti non ordinati (o solo quelli consecutivi) e fare una softmin tra quei k e poi una softmax su tutti i gruppi di k.

