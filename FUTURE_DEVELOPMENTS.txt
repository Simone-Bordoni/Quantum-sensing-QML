General:
- experiment creates global parameters dict, then handles the various functions through global params instead of the various self.constants
- global parameters can be given as an input to create an experiment both if fully or partially populated

Sweep:
- vmapped
- on all experimental and setup parameters through global parameters
- as many dimensions as needed

Optimization:
- on measurement time
- speedup
- custom precision annealing

Visualization:
- Time evolution where various subsystems and observables can be choosen and toggled
- multidimensional sweep visualization
- clean and automize optimization dashboard (wheter or not detection metrics should be plotted and wheter or not visualization and metric should both be plotted)